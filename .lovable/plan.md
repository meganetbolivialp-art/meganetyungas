# Plan: Repair and Deployment Fixes for VPS Web Service

The user provided logs showing:
1. `nginx.service` is **Active (running)** and listening on port 80.
2. `meganet-web.service` **could not be found**. This is the primary reason for `ERR_CONNECTION_REFUSED` or 502 errors, as Nginx has no backend to proxy to on port 3000.
3. Several Supabase containers (`storage`, `auth`, `studio`) are **Restarting** or **Unhealthy**, likely due to missing configuration or dependency failures.
4. `docker-proxy` is listening on port 8000 (Kong), which is good.

## Diagnosis
- **Missing Service:** The `meganet-web.service` unit was not created or was created in a different path/name during the last installation attempt.
- **Supabase Instability:** The `Restarting` status of `auth` and `storage` usually points to a database connection issue or incorrect environment variables (JWT keys, etc.) in the `supabase/.env` file.
- **Deployment Path:** The logs show the user is in `/opt/meganet-deploy`, but the script `install-meganet.sh` uses `/opt/meganet` as the default `INSTALL_DIR`.

## Proposed Actions

### 1. Fix the Meganet Web Service
I will provide a script to manually recreate and start the `meganet-web.service`. I'll ensure it points to the correct build location (`/opt/meganet/frontend-src` or wherever the build ended up).

### 2. Stabilize Supabase Containers
The "Restarting" status needs investigation. I will provide commands to check the logs of the failing containers:
- `docker logs meganet-supabase-auth-1`
- `docker logs meganet-supabase-storage-1`
Common cause: The `db` container is healthy, but the `auth` container might have wrong credentials or the `JWT_SECRET` doesn't match what the `db` expects if it was partially restored.

### 3. Update the Installation Script
I will modify `deploy/install-meganet.sh` to:
- Be more resilient about service creation.
- Add better logging for the "Step 6 (Frontend)" which seems to have been skipped or failed.
- Ensure `bun run build` completes successfully before creating the service.

### 4. Direct IP Access
Since the user is using `144.91.78.4` (IP) and not a domain, I will verify the Nginx configuration generated by the script doesn't strictly require a `server_name` that doesn't match the IP.

## User Instructions (to be executed on VPS)
I will ask the user to run a specific "Repair Block" to fix the missing service and check Docker logs.

```bash
# Repair the missing web service
cd /opt/meganet/frontend-src && bun run build
# (I will provide the full systemd unit creation command)

# Check why Supabase is failing
sudo docker logs meganet-supabase-auth-1 --tail 50
```

## Questions
1. Did the `bun run build` step finish successfully during your last install? (It usually takes 2-3 minutes).
2. Can you provide the logs for the auth container: `sudo docker logs meganet-supabase-auth-1`?
